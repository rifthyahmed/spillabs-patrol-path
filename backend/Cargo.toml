[package]
name = "patrol_backend"
version = "0.1.0"
edition = "2021"

[dependencies]
actix-web = "4.5.1"      # Last version that works with Rust 1.85
actix-cors = "0.6.5"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
env_logger = "0.10.0"
